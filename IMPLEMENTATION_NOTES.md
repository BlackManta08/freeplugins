// File: src/PluginProcessor.cpp - Update state information methods\n// Add these implementations to PluginProcessor.cpp\n\nvoid FreeAutotuneProcessor::getStateInformation(juce::MemoryBlock& destData)\n{\n    // Create XML state\n    auto xml = std::make_unique<juce::XmlElement>(\"AutotuneState\");\n    \n    // Save parameters\n    xml->setAttribute(\"pitchCorrection\", pitchCorrectionAmount->get());\n    xml->setAttribute(\"scaleType\", scaleType->getIndex());\n    xml->setAttribute(\"bypass\", bypassAutotune->get());\n    xml->setAttribute(\"vibratoAmount\", vibratoAmount->get());\n    xml->setAttribute(\"vibratoRate\", vibratoRate->get());\n    xml->setAttribute(\"formantShift\", formantShift->get());\n    \n    copyXmlToBinary(*xml, destData);\n}\n\nvoid FreeAutotuneProcessor::setStateInformation(const void* data, int sizeInBytes)\n{\n    // Restore parameters from XML state\n    auto xml = getXmlFromBinary(data, sizeInBytes);\n    \n    if (xml != nullptr)\n    {\n        *pitchCorrectionAmount = (float)xml->getDoubleAttribute(\"pitchCorrection\", 0.8);\n        *scaleType = (float)xml->getIntAttribute(\"scaleType\", 0);\n        *bypassAutotune = xml->getBoolAttribute(\"bypass\", false);\n        *vibratoAmount = (float)xml->getDoubleAttribute(\"vibratoAmount\", 0.0);\n        *vibratoRate = (float)xml->getDoubleAttribute(\"vibratoRate\", 5.0);\n        *formantShift = (float)xml->getDoubleAttribute(\"formantShift\", 0.0);\n    }\n}\n